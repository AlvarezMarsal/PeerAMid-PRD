@{
    ViewBag.Title = "AboutUs";
    Layout = "~/Views/Shared/_PeerAMidLayout.cshtml";
}
<style>
    /*
        Responsive Text formula (https://css-tricks.com/snippets/css/fluid-typography/) -

        font-size: calc([minimum size] + ([max size] - [min size]) * ((100vw - [min viewport width]) / ([max viewport width] - [min viewport width])));

        Note: 'calc' isn't fully supported by Internet Explorer, so a less sophisticated font-size calculcation is included for IE.
    */

    .first-text {
        color: white;
        font-size: 2.8vw; /* For IE - originally 41px (PEER-15) */
        font-size: calc(24px + (41 - 24) * ((100vw - 800px) / (1920 - 800))); /* originally 41px (PEER-15) */
        font-weight: bold;
        left: 1%;
        position: absolute;
        right: 69%;
        top: calc(50vh - 15vw); /* originally 29% (PEER-15) */
    }

    .second-text {
        color: white;
        font-size: 1.4vw; /* For IE - originally 24px (PEER-15) */
        font-size: calc(14px + (24 - 14) * ((100vw - 800px) / (1920 - 800))); /* originally 24px (PEER-15) */
        left: 1%;
        position: absolute;
        right: 75%;
        top: 50%;
    }
</style>
<!--Hompage in peerAMid-->
<img src="~/Content/Images/img22.png" style="-moz-transform: translateX(-50%) translateY(-50%); -ms-transform: translateX(-50%) translateY(-50%); -webkit-transform: translateX(-50%) translateY(-50%); background: #000; background-size: cover; height: auto; left: 50%; min-height: 100%; min-width: 100%; position: fixed; top: 50%; transform: translateX(-50%) translateY(-50%); width: auto; z-index: -100;"/>
<p class="first-text">
    Welcome to A&M's
    PeerAMid platform
</p>
<p class="second-text">
    To get started, please select the
    service of interest from the Services
    menu above
</p>